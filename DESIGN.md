# RANGE LAB 设计文档

## 设计目标

提供可在桌面网页反复使用的三维训练闭环，优先验证移动停稳、点射恢复、连续后坐力和预瞄清角。场景与枪械为原创程序化几何资产；默认枪声为公开社区狂徒单发录音，程序合成为加载失败备用，可替换为用户本地音频。

## 架构设计

`main.ts` 管理 DOM、设置、Pointer Lock 和成绩；`Game` 管理固定步长训练；`ShotCadence` 与 `VandalRecoil` 管理可独立测试的出弹、散布热度和后坐力；`RangeScene` 管理 Three.js 渲染和 Rapier 碰撞；`GunAudio` 管理录音、备用合成与音效调度。

输入 -> 120 Hz 运动模拟 -> 碰撞修正 -> 实际速度判定 -> 开枪与射线命中 -> 后坐力增量 -> 渲染与统计。

角色为运动学胶囊体，调用 Rapier character controller 处理地面、墙体、箱体与训练掩体。射击命中按距离选择目标或墙体，子弹不穿透掩体。枪口曳光只负责视觉，射线从视角出发，减少枪模视差干扰。

## 关键决策

- 固定 120 Hz 步长，渲染使用 requestAnimationFrame。射击排程保留小于步长的时间余数，避免射速被步长取整。单帧模拟积压上限 100 ms，极低帧率下模拟时间会慢于现实时间。
- 脱离鼠标锁定、失去窗口焦点或隐藏页面时暂停。暂停清空按键、速度和扳机状态，训练计时与换弹计时冻结。
- 腰射子弹方向使用完整后坐力，镜头只跟随一部分，玩家需下拉鼠标压枪。停火后热度连续恢复，避免快速点按重置全部误差。移动散布基于碰撞后实际速度和当前慢走状态。
- 急停模式以移动距离和速度约束有效击杀；普通命中率单独反映弹着结果。停稳时间仅统计有实际移动到停稳转换后的射击。
- 清角模式以目标头部可见性转换记录预瞄角度。统计只在玩家移动时采样；视野判断为近似角度范围，尚未包含多目标同时暴露分析。
- 原生 DOM 足以覆盖首版设置、弹窗和成绩，避免另建与每帧模拟重复的 React 状态层。
- 存储只包含本机设置与最近 30 轮成绩。记录保存难度、后坐力与武器配置版本。旧版本记录不计入当前配置最佳值，当前没有跨配置排名和账号同步。

## 武器参数与限制

基础弹匣 25、射速 9.75/s、换弹 2.5s、基础点射恢复阈值 0.375s；首发散布 0.25°、站立连射散布上限 1°，移动另加误差。取消固定正弦摆动，采用前 6 发保护与后续横向随机切换。数值来源、概率时钟的近似解释、后坐力角度和长连射恢复假设详见 [狂徒校准记录](docs/vandal-calibration.md)。

默认枪声为 3 个 0.82 秒录音片段，在本地预加载并经 Web Audio 解码，按出弹时间调度且不连续重复同一片段。音频峰值、起音和解码由测试检查，失败时切换到预生成的短促爆裂噪声。导入音频优先。社区录音不是 Riot 官方授权分发资产，来源和发布前的权利边界记录在 [SOURCE.md](public/audio/vandal/SOURCE.md)，未做原版盲听对比。

FOV 设置表示 16:9 参考画幅的水平角，再换算垂直投影。浏览器缩放、系统鼠标加速和设备 DPI 均可能影响灵敏度。首版无 ADS、蹲跳、移动敌人、武器切换、多人网络或原版地图复刻。

## 安全考量

威胁模型包括受污染的 localStorage、恶意或过大的音频文件、动态内容注入和外部资产失效。

- 持久化设置限制字段类型、有限数值和范围；历史记录校验模式、日期与统计数值。
- 动态文本使用 textContent 和 DOM 节点，不通过 HTML 插值或 eval 执行输入。文件名作为普通文本展示。
- 本地音频最大 5 MB，解码后限制 5 秒。只在内存中使用，不上传、不执行音频内容。
- 无认证凭证、远程写入、用户行为上报或排行榜信任边界。localStorage 可自行修改，不适合作为竞技排名依据。
- Google Fonts 为可选字体请求，失败时回退系统字体；核心场景、枪械与音效不依赖运行时外部媒体加载。音效仅请求本地静态 WAV，加载或解码错误回退到合成音。
- 静态站点上线应启用 HTTPS，以支持鼠标锁定等浏览器能力。

## 验证

纯逻辑测试覆盖停止速度、反向制动、散布、后坐力、设置校验和统计边界。Playwright 使用真实 WebGL 画布与 Pointer Lock 测试射击、移动、自动换弹、暂停、结算、清角遮挡、音频导入和保存，并输出桌面及移动端截图。

## 变更历史

- 2026-09-05：初版实现三项训练模式、原创靶场、步枪近似模型、合成与导入枪声、设置和本地记录。
- 2026-09-05：按用户要求校准狂徒腰射。调整散布、精度速度阈值、恢复状态、随机横向切换、镜头与弹道分离、腿部伤害与 150 生命值；提取社区录音单发，增加声音预加载、试听与失败回退。修改影响射击、统计、音效和对应测试，不改训练模式或地图。

## 闪光训练扩展

`src/flash.ts` 管理基于训练时间的随机调度、最终朝向判定、残留致盲与统计；`Game` 在固定步进内生成世界坐标闪光源，以相机朝向及场景掩体射线判定爆闪。闪光源出现后保持位置，转身不会让它跟随镜头。每轮开始快照 `flashEnabled`，暂停不推进时间，结束只保留已爆闪记录并累计实际经历的致盲时间。DOM 遮罩不拦截鼠标，暂停与结束时隐藏。

完整机制与边界见 [闪光训练](docs/flash-training.md)。代码职责和存储约定见 [实现结构](docs/implementation.md)，后续开发按 [迭代与验证](docs/iteration.md) 定位改动与回归范围。
